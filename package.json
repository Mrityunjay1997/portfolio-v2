{
  "name": "mk-ai-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered portfolio for Senior Python Backend and Full Stack Developer Mrityunjay Kumar.",
  "scripts": {
    "start": "npm --prefix frontend run start",
    "build": "npm --prefix frontend run build",
    "backend:dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend",
    "backend:check": "python3 -m compileall backend/app",
    "check": "npm --prefix frontend run build && python3 -m compileall backend/app"
  },
  "engines": {
    "node": ">=18.19.1 <23",
    "npm": ">=10.0.0",
    "python": ">=3.11"
  }
}
