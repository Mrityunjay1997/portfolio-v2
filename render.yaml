services:
  # ── Redis ──────────────────────────────────────────────────────────────────
  - type: redis
    name: mk-ai-redis
    region: oregon
    plan: free
    ipAllowList: []

  # ── Backend (FastAPI) ──────────────────────────────────────────────────────
  - type: web
    name: mk-ai-backend
    runtime: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_ENV
        value: production
      - key: FRONTEND_ORIGIN
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mk-ai-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: mk-ai-redis
          type: redis
          property: connectionString
      - key: OPENAI_API_KEY
        sync: false
      - key: CONTACT_TO_EMAIL
        value: krmrityunjay13@gmail.com
      - key: CHROMA_PERSIST_DIRECTORY
        value: /tmp/chroma

  # ── Frontend (Angular via Nginx) ───────────────────────────────────────────
  - type: web
    name: mk-ai-frontend
    runtime: docker
    region: oregon
    plan: free
    rootDir: frontend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BACKEND_HOST
        fromService:
          name: mk-ai-backend
          type: web
          property: host

databases:
  - name: mk-ai-postgres
    region: oregon
    plan: free
    databaseName: portfolio
    user: portfolio
